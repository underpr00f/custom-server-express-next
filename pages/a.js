import { Layout } from "../components/Layout";

export default () => {
    const title = "A page"
    const description = "A description"
    return (
        <Layout
            title={title}
            description={description}
        >
            <div>aaaa</div>
        </Layout>
    )
}
