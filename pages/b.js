import { Layout } from "../components/Layout";

export default () => {
    const title = "B page"
    const description = "B description"
    return (
        <Layout
            title={title}
            description={description}
        >
            <div>BBBB</div>
        </Layout>
    )
}
